# CheweHub 前端迁移指南

## 项目配置概览

### 核心技术栈
- **构建工具**: Vite 6.3.6
- **Vue版本**: Vue 3.5.16
- **UI框架**: Element Plus 2.9.9
- **CSS框架**: Tailwind CSS 4.1.11 (使用 @tailwindcss/vite 插件)
- **状态管理**: Pinia 3.0.2
- **路由**: Vue Router 4.5.1
- **HTTP客户端**: Axios 1.9.0

### 完整依赖列表

#### 生产依赖 (dependencies)

```json
{
  "@element-plus/icons-vue": "2.3.1",
  "@tailwindcss/vite": "^4.1.11",
  "@vueup/vue-quill": "1.2.0",
  "@vueuse/core": "13.3.0",
  "animejs": "^4.0.2",
  "axios": "1.9.0",
  "clipboard": "2.0.11",
  "echarts": "6.0.0",
  "element-plus": "2.9.9",
  "file-saver": "2.0.5",
  "fuse.js": "6.6.2",
  "js-beautify": "1.14.11",
  "js-cookie": "3.0.5",
  "jsencrypt": "3.3.2",
  "nprogress": "0.2.0",
  "pinia": "3.0.2",
  "splitpanes": "4.0.4",
  "vue": "3.5.16",
  "vue-cropper": "1.1.1",
  "vue-router": "4.5.1",
  "vue3-cookies": "^1.0.6",
  "vuedraggable": "4.1.0"
}
```

#### 开发依赖 (devDependencies)

```json
{
  "@tailwindcss/cli": "^4.1.11",
  "@vitejs/plugin-vue": "5.2.4",
  "autoprefixer": "^10.4.21",
  "postcss": "^8.5.6",
  "sass-embedded": "1.89.1",
  "tailwindcss": "^4.1.11",
  "unplugin-auto-import": "0.18.6",
  "unplugin-vue-setup-extend-plus": "1.0.1",
  "vite": "6.3.6",
  "vite-plugin-compression": "0.5.1",
  "vite-plugin-svg-icons": "2.0.1",
  "vite-plugin-vue-devtools": "^8.0.5"
}
```

#### 依赖覆盖 (overrides)

```json
{
  "quill": "2.0.2"
}
```

### 依赖说明

**核心框架：**
- `vue@3.5.16` - Vue 3 框架
- `vue-router@4.5.1` - 路由管理
- `pinia@3.0.2` - 状态管理

**UI框架：**
- `element-plus@2.9.9` - UI组件库
- `@element-plus/icons-vue@2.3.1` - Element Plus 图标

**样式：**
- `tailwindcss@^4.1.11` - Tailwind CSS 4.x
- `@tailwindcss/vite@^4.1.11` - Tailwind Vite 插件
- `sass-embedded@1.89.1` - Sass 编译器

**HTTP请求：**
- `axios@1.9.0` - HTTP 客户端

**工具库：**
- `js-cookie@3.0.5` - Cookie 操作（用于Token存储）
- `nprogress@0.2.0` - 进度条（路由守卫使用）
- `file-saver@2.0.5` - 文件下载
- `jsencrypt@3.3.2` - RSA加密（登录加密）
- `clipboard@2.0.11` - 剪贴板操作

**图表：**
- `echarts@6.0.0` - 图表库

**其他：**
- `@vueuse/core@13.3.0` - Vue组合式API工具集
- `@vueup/vue-quill@1.2.0` - 富文本编辑器
- `vue-cropper@1.1.1` - 图片裁剪
- `vuedraggable@4.1.0` - 拖拽排序
- `splitpanes@4.0.4` - 面板分割
- `fuse.js@6.6.2` - 模糊搜索
- `animejs@^4.0.2` - 动画库
- `js-beautify@1.14.11` - 代码格式化

## 迁移步骤

### 1. 创建新的 Vue3 项目

```bash
# 使用 Vite 创建 Vue3 项目
npm create vite@latest new-project -- --template vue
cd new-project
npm install
```

### 2. 安装核心依赖

```bash
# UI框架和图标
npm install element-plus @element-plus/icons-vue

# Tailwind CSS 4.x (使用新的Vite插件)
npm install -D tailwindcss@^4.1.11 @tailwindcss/vite@^4.1.11 @tailwindcss/cli@^4.1.11

# 状态管理和路由
npm install pinia vue-router@4

# HTTP客户端
npm install axios

# 工具库
npm install js-cookie@3.0.5
npm install nprogress@0.2.0
npm install file-saver@2.0.5
npm install jsencrypt@3.3.2
npm install clipboard@2.0.11

# 图表和编辑器
npm install echarts@6.0.0
npm install @vueup/vue-quill@1.2.0

# 其他依赖
npm install @vueuse/core@13.3.0
npm install vue3-cookies@^1.0.6
npm install vue-cropper@1.1.1
npm install vuedraggable@4.1.0
npm install splitpanes@4.0.4
npm install fuse.js@6.6.2
npm install animejs@^4.0.2
npm install js-beautify@1.14.11

# 开发依赖
npm install -D unplugin-auto-import@0.18.6
npm install -D unplugin-vue-setup-extend-plus@1.0.1
npm install -D vite-plugin-compression@0.5.1
npm install -D vite-plugin-svg-icons@2.0.1
npm install -D vite-plugin-vue-devtools@^8.0.5
```

### 3. 配置 Tailwind CSS

#### 3.1 创建 `tailwind.config.js`

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      // Typography 主题颜色
      colors: {
        'typography': {
          'bg': '#ffffff',           // 背景色
          'fg': '#2e405b',           // 前景色（文字颜色）
          'fg-dark': '#1a2d42',      // 深色前景（focus 状态，darken 30%）
          'fg-light': '#3d5670',     // 浅色前景（hover 状态，lighten 10%）
        },
      },
      
      fontFamily: {
        // Mulish 可变字体家族（仅使用 Mulish，无备用字体）
        'mulish': ['Mulish'],
        
        // Dream Han 字体家族（仅使用自定义字体，无备用字体）
        'dream-sans-w14': ['Dream Han Sans W14'],
        'dream-sans-w20': ['Dream Han Sans W20'],
        'dream-serif-w20': ['Dream Han Serif W20'],
        
        // 混合中英文字体家族（仅使用配置的字体）
        'mix-sans': [
          'Mulish',
          'Dream Han Sans W20'
        ],
        
        // 混合中英文衬线字体家族（仅使用配置的字体）
        'mix-serif': [
          'Dream Han Serif W20'
        ],
        
        // 覆盖默认字体，只使用自定义字体（无备用字体）
        'sans': ['Mulish', 'Dream Han Sans W20'],
        'serif': ['Dream Han Serif W20'],
        'mono': ['Mulish'],
      },
    },
  },
  plugins: [],
  corePlugins: {
    preflight: true,
  },
}
```

#### 3.2 创建 `src/assets/styles/tailwind.css`

```css
@import 'tailwindcss';
@import './fonts.css';
@import './typography-colors.css';
```

#### 3.3 创建 `src/assets/styles/fonts.css`

```css
/* 自定义字体定义 */

/* Mulish 可变字体 - 常规 */
@font-face {
  font-family: 'Mulish';
  src: url('../fonts/Mulish-VariableFont_wght.ttf') format('truetype');
  font-weight: 100 900;
  font-style: normal;
  font-display: swap;
}

/* Mulish 可变字体 - 斜体 */
@font-face {
  font-family: 'Mulish';
  src: url('../fonts/Mulish-Italic-VariableFont_wght.ttf') format('truetype');
  font-weight: 100 900;
  font-style: italic;
  font-display: swap;
}

/* Dream Han Sans W14 (思源黑体 W14) */
@font-face {
  font-family: 'Dream Han Sans W14';
  src: url('../fonts/DreamHanSans-W14.ttc');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

/* Dream Han Sans W20 (思源黑体 W20) */
@font-face {
  font-family: 'Dream Han Sans W20';
  src: url('../fonts/DreamHanSans-W20.ttc');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}

/* Dream Han Serif W20 (思源宋体 W20) */
@font-face {
  font-family: 'Dream Han Serif W20';
  src: url('../fonts/DreamHanSerif-W20.ttc');
  font-weight: normal;
  font-style: normal;
  font-display: swap;
}
```

#### 3.4 创建 `src/assets/styles/typography-colors.css`

```css
/* Typography 主题颜色配置 */
:root {
  /* Typography 颜色变量 */
  --typography-bg: #ffffff;
  --typography-fg: #2e405b;
  --typography-fg-dark: #1a2d42;  /* darken 30% */
  --typography-fg-light: #3d5670; /* lighten 10% */
  
  /* CSS 变量，可在 JavaScript 中使用 */
  --color-typography-bg: var(--typography-bg);
  --color-typography-fg: var(--typography-fg);
  --color-typography-fg-dark: var(--typography-fg-dark);
  --color-typography-fg-light: var(--typography-fg-light);
}
```

#### 3.5 复制字体文件

将以下字体文件复制到 `src/assets/fonts/` 目录：
- `Mulish-VariableFont_wght.ttf`
- `Mulish-Italic-VariableFont_wght.ttf`
- `DreamHanSans-W14.ttc`
- `DreamHanSans-W20.ttc`
- `DreamHanSerif-W20.ttc`

### 4. 配置 Vite

#### 4.1 更新 `vite.config.js`

```javascript
import { defineConfig, loadEnv } from 'vite'
import path from 'path'
import vue from '@vitejs/plugin-vue'
import tailwindcss from '@tailwindcss/vite'

const baseUrl = 'http://localhost:38080' // 后端接口地址

export default defineConfig(({ mode, command }) => {
  const env = loadEnv(mode, process.cwd())
  const { VITE_APP_ENV } = env
  
  return {
    // 部署路径
    base: VITE_APP_ENV === 'production' ? '/' : '/',
    
    plugins: [
      vue(),
      tailwindcss({
        config: './tailwind.config.js'
      }),
    ],
    
    resolve: {
      alias: {
        '~': path.resolve(__dirname, './'),
        '@': path.resolve(__dirname, './src')
      },
      extensions: ['.mjs', '.js', '.ts', '.jsx', '.tsx', '.json', '.vue']
    },
    
    // 打包配置
    build: {
      sourcemap: command === 'build' ? false : 'inline',
      outDir: 'dist',
      assetsDir: 'assets',
      chunkSizeWarningLimit: 2000,
      rollupOptions: {
        output: {
          chunkFileNames: 'static/js/[name]-[hash].js',
          entryFileNames: 'static/js/[name]-[hash].js',
          assetFileNames: 'static/[ext]/[name]-[hash].[ext]'
        }
      }
    },
    
    // 开发服务器配置
    server: {
      port: 39090,
      host: true,
      open: true,
      proxy: {
        // API代理
        '/dev-api': {
          target: baseUrl,
          changeOrigin: true,
          rewrite: (p) => p.replace(/^\/dev-api/, '')
        },
        // springdoc proxy
        '^/v3/api-docs/(.*)': {
          target: baseUrl,
          changeOrigin: true,
        }
      }
    },
    
    css: {
      postcss: {
        plugins: [
          {
            postcssPlugin: 'internal:charset-removal',
            AtRule: {
              charset: (atRule) => {
                if (atRule.name === 'charset') {
                  atRule.remove()
                }
              }
            }
          }
        ]
      }
    }
  }
})
```

### 5. 配置环境变量

创建 `.env.development` 文件：

```env
# 开发环境配置
VITE_APP_ENV = development
VITE_APP_BASE_API = /dev-api
```

创建 `.env.production` 文件：

```env
# 生产环境配置
VITE_APP_ENV = production
VITE_APP_BASE_API = /prod-api
```

### 6. 配置 Element Plus

#### 6.1 更新 `src/main.js`

```javascript
import { createApp } from 'vue'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import 'element-plus/theme-chalk/dark/css-vars.css'
import locale from 'element-plus/es/locale/lang/zh-cn'
import Cookies from 'js-cookie'

import '@/assets/styles/tailwind.css' // Tailwind CSS

import App from './App.vue'
import router from './router'
import store from './store'

const app = createApp(App)

app.use(router)
app.use(store)

// 使用 Element Plus 并且设置全局的大小
app.use(ElementPlus, {
  locale: locale,
  // 支持 large、default、small
  size: Cookies.get('size') || 'default'
})

app.mount('#app')
```

### 7. 配置 Axios 请求和鉴权

#### 7.1 创建 Token 管理工具 `src/utils/auth.js`

```javascript
import Cookies from 'js-cookie'

const TokenKey = 'Admin-Token'

export function getToken() {
  return Cookies.get(TokenKey)
}

export function setToken(token) {
  return Cookies.set(TokenKey, token)
}

export function removeToken() {
  return Cookies.remove(TokenKey)
}
```

#### 7.2 创建缓存插件 `src/plugins/cache.js`

```javascript
const sessionCache = {
  set (key, value) {
    if (!sessionStorage) {
      return
    }
    if (key != null && value != null) {
      sessionStorage.setItem(key, value)
    }
  },
  get (key) {
    if (!sessionStorage) {
      return null
    }
    if (key == null) {
      return null
    }
    return sessionStorage.getItem(key)
  },
  setJSON (key, jsonValue) {
    if (jsonValue != null) {
      this.set(key, JSON.stringify(jsonValue))
    }
  },
  getJSON (key) {
    const value = this.get(key)
    if (value != null) {
      return JSON.parse(value)
    }
    return null
  },
  remove (key) {
    sessionStorage.removeItem(key)
  }
}

const localCache = {
  set (key, value) {
    if (!localStorage) {
      return
    }
    if (key != null && value != null) {
      localStorage.setItem(key, value)
    }
  },
  get (key) {
    if (!localStorage) {
      return null
    }
    if (key == null) {
      return null
    }
    return localStorage.getItem(key)
  },
  setJSON (key, jsonValue) {
    if (jsonValue != null) {
      this.set(key, JSON.stringify(jsonValue))
    }
  },
  getJSON (key) {
    const value = this.get(key)
    if (value != null) {
      return JSON.parse(value)
    }
    return null
  },
  remove (key) {
    localStorage.removeItem(key)
  }
}

export default {
  session: sessionCache,
  local: localCache
}
```

#### 7.3 创建错误码映射 `src/utils/errorCode.js`

```javascript
export default {
  '401': '认证失败，无法访问系统资源',
  '403': '当前操作没有权限',
  '404': '访问资源不存在',
  'default': '系统未知错误，请反馈给管理员'
}
```

#### 7.4 创建请求工具 `src/utils/request.js`

```javascript
import axios from 'axios'
import { ElNotification, ElMessageBox, ElMessage, ElLoading } from 'element-plus'
import { getToken } from '@/utils/auth'
import errorCode from '@/utils/errorCode'
import { tansParams, blobValidate } from '@/utils/ruoyi'
import cache from '@/plugins/cache'
import { saveAs } from 'file-saver'
import useUserStore from '@/store/modules/user'

let downloadLoadingInstance
export let isRelogin = { show: false }

axios.defaults.headers['Content-Type'] = 'application/json;charset=utf-8'

// 创建axios实例
const service = axios.create({
  baseURL: import.meta.env.VITE_APP_BASE_API,
  timeout: 10000
})

// request拦截器
service.interceptors.request.use(config => {
  // 是否需要设置 token
  const isToken = (config.headers || {}).isToken === false
  // 是否需要防止数据重复提交
  const isRepeatSubmit = (config.headers || {}).repeatSubmit === false
  
  // 添加 Token 到请求头
  if (getToken() && !isToken) {
    config.headers['Authorization'] = 'Bearer ' + getToken()
  }
  
  // GET请求参数处理
  if (config.method === 'get' && config.params) {
    let url = config.url + '?' + tansParams(config.params)
    url = url.slice(0, -1)
    config.params = {}
    config.url = url
  }
  
  // POST/PUT 请求防重复提交
  if (!isRepeatSubmit && (config.method === 'post' || config.method === 'put')) {
    const requestObj = {
      url: config.url,
      data: typeof config.data === 'object' ? JSON.stringify(config.data) : config.data,
      time: new Date().getTime()
    }
    const requestSize = Object.keys(JSON.stringify(requestObj)).length
    const limitSize = 5 * 1024 * 1024 // 限制5M
    
    if (requestSize >= limitSize) {
      console.warn(`[${config.url}]: 请求数据大小超出允许的5M限制，无法进行防重复提交验证。`)
      return config
    }
    
    const sessionObj = cache.session.getJSON('sessionObj')
    if (sessionObj === undefined || sessionObj === null || sessionObj === '') {
      cache.session.setJSON('sessionObj', requestObj)
    } else {
      const s_url = sessionObj.url
      const s_data = sessionObj.data
      const s_time = sessionObj.time
      const interval = 1000 // 间隔时间1秒
      
      if (s_data === requestObj.data && requestObj.time - s_time < interval && s_url === requestObj.url) {
        const message = '数据正在处理，请勿重复提交'
        console.warn(`[${s_url}]: ` + message)
        return Promise.reject(new Error(message))
      } else {
        cache.session.setJSON('sessionObj', requestObj)
      }
    }
  }
  
  return config
}, error => {
  console.log(error)
  return Promise.reject(error)
})

// 响应拦截器
service.interceptors.response.use(res => {
  const code = res.data.code || 200
  const msg = errorCode[code] || res.data.msg || errorCode['default']
  
  // 二进制数据直接返回
  if (res.request.responseType === 'blob' || res.request.responseType === 'arraybuffer') {
    return res.data
  }
  
  // 401 未授权，跳转登录
  if (code === 401) {
    if (!isRelogin.show) {
      isRelogin.show = true
      ElMessageBox.confirm('登录状态已过期，您可以继续留在该页面，或者重新登录', '系统提示', {
        confirmButtonText: '重新登录',
        cancelButtonText: '取消',
        type: 'warning'
      }).then(() => {
        isRelogin.show = false
        useUserStore().logOut().then(() => {
          location.href = '/index'
        })
      }).catch(() => {
        isRelogin.show = false
      })
    }
    return Promise.reject('无效的会话，或者会话已过期，请重新登录。')
  } else if (code === 500) {
    ElMessage({ message: msg, type: 'error' })
    return Promise.reject(new Error(msg))
  } else if (code === 601) {
    ElMessage({ message: msg, type: 'warning' })
    return Promise.reject(new Error(msg))
  } else if (code !== 200) {
    ElNotification.error({ title: msg })
    return Promise.reject('error')
  } else {
    return Promise.resolve(res.data)
  }
}, error => {
  console.log('err' + error)
  let { message } = error
  if (message == "Network Error") {
    message = "后端接口连接异常"
  } else if (message.includes("timeout")) {
    message = "系统接口请求超时"
  } else if (message.includes("Request failed with status code")) {
    message = "系统接口" + message.substr(message.length - 3) + "异常"
  }
  ElMessage({ message: message, type: 'error', duration: 5 * 1000 })
  return Promise.reject(error)
})

// 通用下载方法
export function download(url, params, filename, config) {
  downloadLoadingInstance = ElLoading.service({
    text: "正在下载数据，请稍候",
    background: "rgba(0, 0, 0, 0.7)",
  })
  return service.post(url, params, {
    transformRequest: [(params) => { return tansParams(params) }],
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    responseType: 'blob',
    ...config
  }).then(async (data) => {
    const isBlob = blobValidate(data)
    if (isBlob) {
      const blob = new Blob([data])
      saveAs(blob, filename)
    } else {
      const resText = await data.text()
      const rspObj = JSON.parse(resText)
      const errMsg = errorCode[rspObj.code] || rspObj.msg || errorCode['default']
      ElMessage.error(errMsg)
    }
    downloadLoadingInstance.close()
  }).catch((r) => {
    console.error(r)
    ElMessage.error('下载文件出现错误，请联系管理员！')
    downloadLoadingInstance.close()
  })
}

export default service
```

#### 7.5 请求方式说明

**请求配置：**
- **Content-Type**: `application/json;charset=utf-8`（默认）
- **超时时间**: 10秒
- **BaseURL**: 通过环境变量 `VITE_APP_BASE_API` 配置

**鉴权方式：**
- **Token存储**: 使用 `js-cookie` 存储在 Cookies 中
- **Token键名**: `Admin-Token`
- **请求头格式**: `Authorization: Bearer {token}`
- **跳过Token**: 在请求配置中添加 `headers: { isToken: false }`

**特殊功能：**
- **防重复提交**: POST/PUT 请求默认启用，1秒内相同请求会被拦截
- **跳过防重复**: 在请求配置中添加 `headers: { repeatSubmit: false }`
- **GET参数处理**: 自动将 params 转换为 URL 查询字符串
- **文件下载**: 使用 `download()` 方法，支持 blob 响应类型

#### 7.6 路由守卫配置 `src/permission.js`

```javascript
import router from './router'
import { ElMessage } from 'element-plus'
import NProgress from 'nprogress'
import 'nprogress/nprogress.css'
import { getToken } from '@/utils/auth'
import { isHttp, isPathMatch } from '@/utils/validate'
import { isRelogin } from '@/utils/request'
import useUserStore from '@/store/modules/user'
import useSettingsStore from '@/store/modules/settings'
import usePermissionStore from '@/store/modules/permission'

NProgress.configure({ showSpinner: false })

const whiteList = ['/login', '/register']

const isWhiteList = (path) => {
  return whiteList.some(pattern => isPathMatch(pattern, path))
}

router.beforeEach((to, from, next) => {
  NProgress.start()
  if (getToken()) {
    to.meta.title && useSettingsStore().setTitle(to.meta.title)
    if (to.path === '/login') {
      next({ path: '/' })
      NProgress.done()
    } else if (isWhiteList(to.path)) {
      next()
    } else {
      if (useUserStore().roles.length === 0) {
        isRelogin.show = true
        useUserStore().getInfo().then(() => {
          isRelogin.show = false
          usePermissionStore().generateRoutes().then(accessRoutes => {
            accessRoutes.forEach(route => {
              if (!isHttp(route.path)) {
                router.addRoute(route)
              }
            })
            next({ ...to, replace: true })
          })
        }).catch(err => {
          useUserStore().logOut().then(() => {
            ElMessage.error(err)
            next({ path: '/' })
          })
        })
      } else {
        next()
      }
    }
  } else {
    if (isWhiteList(to.path)) {
      next()
    } else {
      next(`/login?redirect=${to.fullPath}`)
      NProgress.done()
    }
  }
})

router.afterEach(() => {
  NProgress.done()
})
```

### 8. 配置 Pinia

创建 `src/store/index.js`：

```javascript
import { createPinia } from 'pinia'

const pinia = createPinia()

export default pinia
```

### 9. 配置 Vue Router

创建 `src/router/index.js`（根据实际路由配置）：

```javascript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  // 根据实际路由配置
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

### 10. 迁移项目文件

需要迁移的核心文件/目录：

```
src/
├── api/              # API接口定义
├── assets/           # 静态资源
│   ├── fonts/        # 字体文件
│   ├── icons/        # 图标文件
│   └── styles/       # 样式文件
├── components/       # 公共组件
├── directive/        # 自定义指令
├── layout/           # 布局组件
├── plugins/          # 插件
├── router/           # 路由配置
├── store/            # 状态管理
├── utils/            # 工具函数
└── views/            # 页面组件
```

### 11. 更新 package.json scripts

```json
{
  "scripts": {
    "dev": "vite",
    "build:prod": "vite build",
    "build:stage": "vite build --mode staging",
    "preview": "vite preview"
  }
}
```

## 请求和鉴权配置说明

### 请求方式

**标准请求示例：**
```javascript
import request from '@/utils/request'

// GET 请求
request.get('/api/user/list', { params: { page: 1, size: 10 } })

// POST 请求
request.post('/api/user/add', { name: 'test', age: 18 })

// PUT 请求
request.put('/api/user/update', { id: 1, name: 'test' })

// DELETE 请求
request.delete('/api/user/1')
```

**跳过Token的请求：**
```javascript
request.get('/api/public/info', {
  headers: { isToken: false }
})
```

**跳过防重复提交：**
```javascript
request.post('/api/user/add', data, {
  headers: { repeatSubmit: false }
})
```

**文件下载：**
```javascript
import { download } from '@/utils/request'

download('/api/file/download', { fileName: 'test.xlsx' }, 'test.xlsx')
```

### 鉴权方式

**Token存储：**
- 存储位置：Cookies
- 键名：`Admin-Token`
- 管理工具：`src/utils/auth.js`

**Token使用流程：**
1. 登录成功后，调用 `setToken(token)` 存储Token
2. 每个请求自动在请求头添加 `Authorization: Bearer {token}`
3. 401响应时自动跳转登录页
4. 登出时调用 `removeToken()` 清除Token

**路由守卫：**
- 白名单：`/login`, `/register`（无需Token）
- 有Token但访问登录页：重定向到首页
- 无Token访问受保护页面：重定向到登录页（带redirect参数）
- 动态路由：根据用户权限动态生成路由表

## 重要注意事项

### 1. Tailwind CSS 4.x 变化

- 使用 `@tailwindcss/vite` 插件替代传统的 PostCSS 配置
- 不需要单独的 `postcss.config.js` 文件
- 使用 `@import 'tailwindcss'` 替代 `@tailwind` 指令

### 2. 后端接口配置

- 开发环境：通过 Vite 代理 `/dev-api` 到 `http://localhost:38080`
- 生产环境：需要配置实际的 API 地址
- API 基础路径通过环境变量 `VITE_APP_BASE_API` 配置

### 3. 样式处理

- 项目使用纯 Tailwind CSS，移除了 SCSS 依赖（除了部分遗留文件）
- 自定义字体和颜色主题通过 CSS 变量和 Tailwind 配置实现
- 确保字体文件路径正确

### 4. Element Plus 集成

- 需要引入 Element Plus 的样式文件
- 支持暗色主题（dark mode）
- 全局配置中文语言包

### 5. 构建优化

- 使用 Vite 的代码分割和压缩
- 静态资源使用 hash 命名
- 生产环境关闭 sourcemap

## 验证清单

- [ ] Tailwind CSS 样式正常加载
- [ ] 自定义字体正常显示
- [ ] Element Plus 组件正常使用
- [ ] API 请求能正常代理到后端
- [ ] 路由跳转正常
- [ ] 状态管理正常
- [ ] 构建打包成功

## 常见问题

### Q: Tailwind CSS 样式不生效？
A: 确保在 `main.js` 中正确引入了 `tailwind.css`，并且 `tailwind.config.js` 的 `content` 配置包含了所有需要扫描的文件。

### Q: 字体文件找不到？
A: 检查字体文件路径是否正确，确保字体文件已复制到 `src/assets/fonts/` 目录。

### Q: API 请求失败？
A: 检查环境变量配置和 Vite 代理配置，确保后端服务正常运行。

### Q: Element Plus 组件样式异常？
A: 确保引入了 Element Plus 的 CSS 文件，并且版本匹配。

## 参考文件

- `cheweHub-ui/package.json` - 依赖配置
- `cheweHub-ui/tailwind.config.js` - Tailwind 配置
- `cheweHub-ui/vite.config.js` - Vite 配置
- `cheweHub-ui/src/main.js` - 入口文件
- `cheweHub-ui/src/assets/styles/` - 样式文件

